- [ ] Update src/lib/products.store.ts to fetch from GET /api/produk and sync store
- [ ] Update src/app/dashboard/produk/page.tsx to render using fields from API (nama_product, jenis_oli, peruntukan, cc_motor, kekentalan_oli, harga, stok, deskripsi, image_url)
- [ ] Update src/app/dashboard/page.tsx (Produk Terbaru preview) to use new store fields
- [ ] Update src/components/admin/ProductDialog.tsx to submit to API (PUT/POST) or align with updated store
- [ ] Update any public product listing page that relies on old static data if needed
- [ ] Final check: typescript build and no runtime crashes

